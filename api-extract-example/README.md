# Deprecated — use Supabase Edge Function instead

This folder was an early **Vercel** example. The primary path is now:

**`supabase/functions/analyze-find-photo/`**

See:

- `supabase/functions/README.md` — deploy, secrets, curl, local dev
- Root `README.md` — AI photo analysis section

You do **not** need a Vercel account if you use Supabase Edge Functions.

---

<details>
<summary>Legacy Vercel instructions (optional)</summary>

Deploy `api-extract-example/` on Vercel and set `OPENAI_API_KEY`. Endpoint:

```text
https://YOUR_PROJECT.vercel.app/api/analyze-find-photo
```

</details>
