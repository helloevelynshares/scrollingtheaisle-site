# Processed TikTok food mentions

`tiktok_item_mentions.csv` is generated by:

```bash
python scripts/extract_tiktok_food_mentions.py
```

Requires `bulk_transcripts.csv` at the repo root or `data/raw/bulk_transcripts.csv`.

Until that file is added, only the header row is present.
