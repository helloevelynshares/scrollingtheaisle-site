# Production-derived candidate cases

Place reviewed production failures and interesting clarifies here before promoting into a frozen baseline.

Do not modify `evals/entity-resolution/catalog-resolution-v1.jsonl` in place.
